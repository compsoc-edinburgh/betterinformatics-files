repos:
  - repo: local
    hooks:
      - id: prettier
        name: prettier
        language: system
        entry: yarn run prettier --write --
        pass_filenames: true
        files: "\\.(js|jsx|ts|tsx|json|css)$"
