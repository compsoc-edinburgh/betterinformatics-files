
import React from 'react';
import { render } from 'react-dom';

import Textarea from './src'
var VALUE = 'xxx'

var App = React.createClass({

    onChange: function(value){
        VALUE = value
        this.setState({})
    },

    render: function() {

        var style = {
            display: 'block',
            border: '20px solid red'
        }

        function validate(value){
            return !!value
        }

        var set = function set(){
            VALUE = 'Lorem ipsum Dolor sit in minim nostrud ad Duis in dolor tempor esse dolor et enim veniam ad Excepteur in nulla culpa occaecat Excepteur ullamco sit Ut irure id deserunt velit sint laborum sed sunt et do cupidatat nostrud deserunt.'
            this.setState({})
        }.bind(this)

        return (
            <div className="App" style={{padding: 10}}>
                <Textarea maxHeight={300} autoGrow style={style} name="AAA" placeholder="test" defaultValue={VALUE} xonChange={this.onChange}/>
                <button onClick={set}>set</button>
            </div>
        )
    }
})

render(<App />, document.getElementById('content'))
