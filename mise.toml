[tools]
node = "24.14.1"
prek = "0.3.3"
uv = "0.10.12"
yarn = "4.14.0"

[env]
_.file = "backend/.env.development"
# overwrite relevant env.development envs for non-docker-compose execution
SIP_POSTGRES_DB_SERVER = "localhost"
SIP_S3_FILES_HOST = "localhost"

COMSOL_ASSETS_FOLDER = "../frontend/public/"
IS_DEBUG = "true"
